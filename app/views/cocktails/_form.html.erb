<%= simple_form_for @cocktail do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit %>

<% end %>

<%= link_to cocktails_path  do %>
  <i class="fas fa-chevron-left"></i>
<% end %>
